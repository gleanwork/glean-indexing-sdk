[project]
name = "wikipedia-connector"
version = "0.1.0"
description = "Glean deployment test connector — crawls Wikipedia articles"
requires-python = ">=3.10"

dependencies = [
    "glean-indexing-sdk",
    "requests>=2.31",
]
