# IMPORTANT: Do not commit actual secrets to version control!
# This is a template - use kubectl create secret or external secret management.
#
# Create the secret:
#   kubectl create secret generic connector-secrets \
#     --namespace glean-connectors \
#     --from-literal=GLEAN_INSTANCE=your-instance \
#     --from-literal=GLEAN_INDEXING_API_TOKEN=your-token \
#     --from-literal=SOURCE_API_KEY=your-source-key

apiVersion: v1
kind: Secret
metadata:
  name: connector-secrets
  namespace: glean-connectors
  labels:
    app.kubernetes.io/name: glean-connector
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Replace these with actual values or use kubectl create secret
  GLEAN_INSTANCE: "your-instance"
  GLEAN_INDEXING_API_TOKEN: "your-token"
  SOURCE_API_KEY: "your-source-key"
